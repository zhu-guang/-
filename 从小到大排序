#include <stdio.h>

int main()
{
	int x,y,z,a;
	scanf("%d%d%d",&x,&y,&z);
	if(x>y)         
	a=x,x=y,y=a;	
	if(z<x) 
	a=x,x=z,z=y,y=a;
	else if(z>x&&z<y)
	a=z,z=y,y=a;
	printf("%d %d %d",x,y,z);
	return 0;
}
